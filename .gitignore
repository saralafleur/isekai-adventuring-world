.DS_Store
sourcemap.json
globalTypes.d.luau
*.rbxl
*.rbxlx

# Wally-managed packages are reproducible from wally.lock; don't commit the vendored copies
/Packages/
/DevPackages/
*.rbxl.lock

# local neural TTS model + venv (regenerate via scripts/generate-voice-lines.py header)
.tts-models/
.venv-tts/

# Roblox Open Cloud credentials for scripts/upload-voice-lines.py. NEVER commit:
# anyone with this key can upload assets to the account.
.env
.env.*
